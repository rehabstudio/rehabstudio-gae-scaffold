machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t="gae_scaffold" .

test:
  override:
    - docker run -t -i -v "`pwd`/app:/app" -v "`pwd`/output:/output" gae_scaffold make -C /app test

general:
  artifacts:
    - "output/coverage.xml"
    - "output/nosetests.xml"
