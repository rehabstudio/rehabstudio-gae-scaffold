machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t="gae_scaffold" .

test:
  override:
    - docker run -t -i -v "`pwd`/src:/src" -v "`pwd`/output:/output" gae_scaffold make -C /src test

general:
  artifacts:
    - "output/coverage.xml"
    - "output/nosetests.xml"
